<h1>New Bakery Form</h1>

<%= form_for(@bakery, html: { class: 'form'}) do |f| %>

  <% if @bakery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bakery.errors.count, "error") %> prohibited this bakery from being saved:</h2>

      <ul>
        <% @bakery.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_area :address %>
  </div>

  <div class="field">
    <%= f.label :city %><br />
    <%= f.text_area :city %>
  </div>

  <div class="field">
    <%= f.label :state %><br />
    <%= f.text_area :state %>
  </div>

  <div class="field">
    <%= f.label :zip %><br />
    <%= f.text_area :zip %>
  </div>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :img_url %><br />
    <%= f.text_area :img_url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', bakeries_path %>
